"""
ValueRank Cloud Workers

Python workers for executing LLM operations via stdin/stdout JSON protocol.
These workers are spawned by the TypeScript orchestrator via spawnPython().
"""

__version__ = "0.1.0"
